# Benchmark configuration reference

Generated by `bench/schemadoc` from `schema/experiment.json`, `schema/app.json`, `schema/site.json` -- do not edit.
Regenerate with `./schemadoc --write`; `test_bench.sh` fails if this
file is stale.

A blank meaning is a key the schema does not describe yet; they are
tracked in `schema/undocumented.txt`.  Cross-field rules a schema
cannot state live in `benchlib/experiment.py` and are what
`bench validate` adds on top of what is below.

## Benchmark experiment

What to sweep: which images, which apps, which rank/thread decompositions, and how the cross product is split into jobs. Host-side only -- bench/submit expands this into a flat job.env per job, and the job itself never parses YAML.

### `experiment`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `account` | string |  |  | Scheduler project to charge. --account overrides it; there is deliberately no default, because this is a billable allocation. |
| [`apps`](#experimentapps) | list of object | yes | at least 1 entry |  |
| [`defaults`](#experimentdefaults) | object |  | no other keys | Job-level settings. Anything an app does not override itself. |
| [`images`](#experimentimages) | object | yes | at least 1 key, no other keys | Exactly one of from_make or list. |
| [`omp_variants`](#experimentomp_variants) | list of object |  | at least 1 entry | Defaults to one variant. Crossing two doubles every sweep, and on HPCG the difference measured 2026-08-19 was minor -- opt in per experiment instead. |
| [`placements`](#experimentplacements) | list of object | yes | at least 1 entry | A placement states only what it ASKS for. What it should get -- L3, NUMA and socket footprint -- follows from the topology probed at job start, so there is no expectation field for a checker to assert against. |
| [`profiles`](#experimentprofiles) | object |  | no other keys | Named single cells -- the answer a sweep produced. bench/submit --profile <name> runs only that. bench/collect --emit-profile generates one from results, so the benchmark's output is the production configuration rather than something transcribed by hand. |
| `schema` | `1` | yes |  | Format version of this file. Bumped only for a change that an older bench/submit would misread. |
| `site` | string | yes | matches `/^[a-z][a-z0-9_-]*$/` | Names sites/<site>/site.sh, which supplies the paths and the module bootstrap. |
| [`sweep`](#experimentsweep) | object | yes | no other keys |  |

#### `experiment.apps`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `args` | string |  |  | Appended to the argv. The app's launch hook is the better place for flags. |
| [`env`](#experimentappsenv) | object |  | no other keys | Extra environment for this app's cells, e.g. OSU_BENCHMARK. |
| `label` | string |  | matches `/^[A-Za-z0-9][A-Za-z0-9_.-]*$/` | How this entry is named in job directories and result rows. Defaults to name, and is what distinguishes two entries of the SAME app that differ only in env -- osu_alltoall from osu_allreduce, say. |
| `name` | string | yes |  | Resolved under /container/app.d/<name> in the image, or an absolute path to a bare executable. |
| `scale` | string |  |  |  |
| `target_seconds` | integer |  | at least 1 |  |

##### `experiment.apps.env`

Extra environment for this app's cells, e.g. OSU_BENCHMARK.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `<name>` | string, integer or boolean |  | key matches `/^[A-Za-z_][A-Za-z0-9_]*$/` |  |

#### `experiment.defaults`

Job-level settings. Anything an app does not override itself.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `allow_undersubscribed` | boolean |  |  | Permit a ranks x threads product that leaves cores idle. Off by default: --cpu-bind depth packs consecutively from core 0, so a smaller product crowds every rank onto the low chiplets rather than idling cores -- a wrong answer that looks like a valid data point. |
| `exclusive` | boolean |  | default `true` | Ask the scheduler for every core on each node, whether or not the cells use them all. True by default, and for a benchmark that is the only defensible setting: a co-tenant's work on the same node lands in your timings as noise you cannot see or subtract. Set it false only on a machine whose nodes are shared by design -- Casper's high-throughput nodes -- where holding a whole node means a long wait to drain other people's small jobs. Then the job requests exactly what its cells use, schedules in minutes, and produces numbers that verify the harness rather than measure the hardware. |
| `nodes` | integer |  | at least 1 |  |
| `queue` | string |  |  |  |
| `repeats` | integer |  | at least 1 | Times each cell is run. Reported as min and median, never as a single sample. |
| `scale` | string |  |  | BENCH_SCALE: an app-defined size class (smoke, node, weak, strong). |
| `target_seconds` | integer |  | at least 1 |  |
| `walltime` | string |  | matches `/^[0-9]{1,3}:[0-5][0-9]:[0-5][0-9]$/` |  |

#### `experiment.images`

Exactly one of from_make or list.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `from_make` | string |  | matches `/^[a-z0-9][a-z0-9._-]*$/` | A libexec/Makefile target such as derecho-hpcg, asked via its echo-<target> rule. One definition of 'the six' rather than a copy that drifts. |
| `list` | list of string |  | at least 1 entry |  |

#### `experiment.omp_variants`

Defaults to one variant. Crossing two doubles every sweep, and on HPCG the difference measured 2026-08-19 was minor -- opt in per experiment instead.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `OMP_PLACES` | `cores` \| `threads` \| `sockets` |  |  |  |
| `OMP_PROC_BIND` | `close` \| `spread` \| `master` \| `true` \| `false` |  |  |  |
| `name` | string | yes | matches `/^[A-Za-z0-9][A-Za-z0-9_.-]*$/` |  |

#### `experiment.placements`

A placement states only what it ASKS for. What it should get -- L3, NUMA and socket footprint -- follows from the topology probed at job start, so there is no expectation field for a checker to assert against.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `name` | string | yes | matches `/^[A-Za-z0-9][A-Za-z0-9_.-]*$/` |  |
| `ranks_per_node` | integer | yes | at least 1 |  |
| `threads` | integer | yes | at least 1 |  |

#### `experiment.profiles`

Named single cells -- the answer a sweep produced. bench/submit --profile <name> runs only that. bench/collect --emit-profile generates one from results, so the benchmark's output is the production configuration rather than something transcribed by hand.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| [`<name>`](#experimentprofilesname) | object |  | key matches `/^[A-Za-z0-9][A-Za-z0-9_.-]*$/`, no other keys |  |

##### `experiment.profiles.<name>`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `app` | string |  |  |  |
| `images` | list of string |  | at least 1 entry |  |
| `nodes` | integer |  | at least 1 |  |
| `omp` | string |  |  |  |
| `placement` | string |  |  |  |
| `repeats` | integer |  | at least 1 |  |

#### `experiment.sweep`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `matrix` | list of `images` \| `apps` \| `placements` \| `omp_variants` | yes | at least 1 entry, no duplicates | The axes crossed. Every axis with two or more entries must appear here; bench/validate refuses to drop values silently. |
| `per_job` | list of `images` \| `apps` \| `placements` \| `omp_variants` |  | no duplicates | Which of those axes ONE job iterates. The rest become separate jobs. This is the knob that trades queue wait against job length. |

## Application contract

What an application must declare so the runner can drive it without knowing which one it is. Installed into the image at /container/app.d/<app>/app.yaml by build_<app>.sh, beside the prepare/launch/extract hooks. An app with no hooks at all is legal: an app.yaml with just app and binary gets you wall time and a placement verdict, which is the ten-minute entry point that makes the contract adoptable.

### `app`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `app` | string | yes | matches `/^[a-z0-9][a-z0-9_-]*$/` | The name the runner is asked for. Must match the app.d directory. |
| `binary` | string |  |  | Absolute path inside the image. Required unless there is a launch hook, which may pick a different executable and add flags. |
| [`figures_of_merit`](#appfigures_of_merit) | object |  | no other keys | Ramble's noun, on purpose. Every key that extract may print, plus wall_s which the runner always provides. |
| `notes` | string |  |  | Free text for whoever reads a results directory a year from now: what the numbers mean, and what would make them not mean it. |
| `primary_fom` | string |  |  | Which figure of merit decides a comparison. Must name a key in figures_of_merit. Read host-side by bench/collect, from the app.yaml copied into the results directory. |
| [`requires`](#apprequires) | object |  | no other keys |  |
| [`success_criteria`](#appsuccess_criteria) | object |  | no other keys |  |
| `version` | string |  |  | Recorded in every result row. A @PLACEHOLDER@ is substituted by build_<app>.sh at install time. |

#### `app.figures_of_merit`

Ramble's noun, on purpose. Every key that extract may print, plus wall_s which the runner always provides.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| [`<name>`](#appfigures_of_meritname) | object |  | key matches `/^[A-Za-z_][A-Za-z0-9_]*$/`, no other keys |  |

##### `app.figures_of_merit.<name>`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `better` | `higher` \| `lower` | yes |  | Which direction wins. bench/collect ranks with this; without it a metric is assumed higher-is-better, which is wrong for a latency. |
| `note` | string |  |  |  |
| `provided_by` | `runner` \| `app` |  |  |  |
| `units` | string |  |  |  |

#### `app.requires`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `min_ranks` | integer |  | at least 1 |  |
| `mpi` | boolean |  |  |  |
| `openmp` | boolean |  |  | false means a thread sweep measures nothing for this app -- rank count is the axis that matters. |

#### `app.success_criteria`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `app_self_validates` | boolean |  |  | When true, extract must print valid=true\|false. A valid=false row is recorded but never wins a comparison. |

## Site description

What a cluster provides, stated once. The declarative half of the site contract: scheduler dialect, module bootstrap, node geometry, container bind list and the host-MPI recipe. bench/sitegen turns this into the generated block of sites/<site>/site.sh, which is the only file a job sources -- so nothing on a compute node ever parses YAML. The imperative half is the hand-edited part of that same site.sh: the four per-operator paths, which are a property of the person rather than of the machine.

### `site`

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| [`container`](#sitecontainer) | object | yes | no other keys | How a container is run here. The bind list is site-specific in the strongest sense: apptainer treats a missing bind source as fatal, so binding a filesystem this machine does not have turns 'that mount is absent' into 'the job will not start'. |
| `description` | string |  |  | What this machine is, for whoever reads a results directory a year from now. Copied into the generated block as a comment. |
| [`modules`](#sitemodules) | object | yes | no other keys | The module environment, which is the one thing every job here must do identically and the thing that was copied into five PBS scripts before this file existed. |
| [`mpi`](#sitempi) | object | yes | at least 1 key, no other keys | One entry per container MPI family this site can host. A family absent here is one this machine cannot run, which the runner reports as such rather than attempting and failing inside the container. |
| [`node`](#sitenode) | object | yes | no other keys | What one compute node has. FALLBACKS, not measurements: the job probes lscpu and topology.json carries its answer. These are what can be known before there is a node to ask, which is the only moment an illegal ranks x threads is still cheap to reject. |
| [`scheduler`](#sitescheduler) | object | yes | no other keys | Which batch system, and how a job reaches it. |
| `schema` | `1` | yes |  | Format version of this file. Bumped only for a change that an older bench/sitegen would misread. |
| `site` | string | yes | matches `/^[a-z][a-z0-9_-]*$/` | The name experiments select with site:, the directory under sites/, and the value recorded in every result row. |
| `verified` | boolean |  | default `true` | False means nothing here has been confirmed by a job that actually ran. bench/validate says so out loud rather than letting an untested description pass for a measured one. |

#### `site.container`

How a container is run here. The bind list is site-specific in the strongest sense: apptainer treats a missing bind source as fatal, so binding a filesystem this machine does not have turns 'that mount is absent' into 'the job will not start'.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| [`bind_map`](#sitecontainerbind_map) | object |  | no other keys | host path to in-container path, for a directory that must not land on top of the container's own. /usr/lib64 is the case: bound at its own name it would replace the container's entire system library tree. |
| `binds` | list of string | yes | at least 1 entry | Bound unconditionally. Only paths this machine is guaranteed to have. |
| `binds_if_present` | list of string |  |  | Bound only when the directory exists. Where a filesystem belongs when some nodes have it and some do not -- GPFS and the Cray trees. |
| `lib_dirs` | list of string |  |  | Host library directories put on LD_LIBRARY_PATH inside the container, in this order, after whatever the MPI overlay prepends. An entry containing * is a glob and resolves to its newest match, because a hardcoded version number rots at the next system update and the job would then silently fall back to the container's own library. An entry may begin ${VAR}/ to name a directory a module supplies, and drops out when that variable is unset. A directory that does not exist is dropped rather than left empty: an empty element in LD_LIBRARY_PATH means the working directory. |
| `runtime` | `apptainer` \| `singularity` | yes |  | The container command. |

##### `site.container.bind_map`

host path to in-container path, for a directory that must not land on top of the container's own. /usr/lib64 is the case: bound at its own name it would replace the container's entire system library tree.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `<name>` | string |  | key matches `/^/[^ ]*$/`, matches `/^/[^ ]*$/` | Where the host path appears inside the container. |

#### `site.modules`

The module environment, which is the one thing every job here must do identically and the thing that was copied into five PBS scripts before this file existed.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `bootstrap` | list of string | yes | at least 1 entry | The module commands every job runs first, in order, as the body of bench_site_modules. A list rather than one string because order is the whole content: `module reset` after loading ncarenv is not redundant, since ncarenv changes MODULEPATH and reset then applies the defaults visible under the new path. |
| [`compiler_map`](#sitemodulescompiler_map) | object |  | no other keys | Container compiler tag to host module. The host MPI that displaces the container's must be built with a compatible compiler, and the tag in the image name is the only thing naming which. |
| [`mpi_map`](#sitemodulesmpi_map) | object |  | no other keys | Container MPI family to host module. An empty value means the host MPI is already in the default environment and there is nothing to load -- Cray MPICH on Derecho -- which is not the same as the family being unsupported. |
| [`unload_when`](#sitemodulesunload_when) | object |  | no other keys | Modules to drop for a given MPI family, because a module left loaded by a previous image shadows this one. Dropping openmpi before an mpich image is the case that exists: its mpiexec and its libraries would otherwise displace Cray PALS and the MPICH ABI shim. |

##### `site.modules.compiler_map`

Container compiler tag to host module. The host MPI that displaces the container's must be built with a compatible compiler, and the tag in the image name is the only thing naming which.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `<name>` | string |  | key matches `/^[a-z][a-z0-9]*$/`, matches `/^[A-Za-z0-9][A-Za-z0-9._/-]*$/` | The module to load, version and all. Pin the version where the container's compiler must be matched exactly rather than followed. |

##### `site.modules.mpi_map`

Container MPI family to host module. An empty value means the host MPI is already in the default environment and there is nothing to load -- Cray MPICH on Derecho -- which is not the same as the family being unsupported.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `<name>` | string |  | key matches `/^(openmpi|mpich|mpich3)$/`, matches `/^[A-Za-z0-9._/-]*$/` | The module that provides the host MPI for this container family. Empty means there is nothing to load. |

##### `site.modules.unload_when`

Modules to drop for a given MPI family, because a module left loaded by a previous image shadows this one. Dropping openmpi before an mpich image is the case that exists: its mpiexec and its libraries would otherwise displace Cray PALS and the MPICH ABI shim.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `<name>` | list of string |  | key matches `/^(openmpi|mpich|mpich3)$/`, at least 1 entry | Modules unloaded before this family's is loaded. |

#### `site.mpi`

One entry per container MPI family this site can host. A family absent here is one this machine cannot run, which the runner reports as such rather than attempting and failing inside the container.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| [`<name>`](#sitempiname) | object |  | key matches `/^(openmpi|mpich|mpich3)$/`, no other keys | How this site hosts one container MPI family: which mpiexec dialect places its ranks, which recipe swaps the host library in, and anything that has to be set inside the container for it. |

##### `site.mpi.<name>`

How this site hosts one container MPI family: which mpiexec dialect places its ranks, which recipe swaps the host library in, and anything that has to be set inside the container for it.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| [`env`](#sitempinameenv) | object |  | no other keys | Environment set inside the container for this family. Where a workaround whose reason is specific to this machine goes. |
| `launcher` | `pals` \| `openmpi` \| `srun` | yes |  | Which mpiexec dialect emits this family's placement flags. PALS spells procs-per-node -ppn N and binding --cpu-bind core -d D; Open MPI spells the same two things -N N and --map-by ppr:P:node:pe=D --bind-to core. |
| `notes` | string |  |  | Free text. What was checked, and what would make it stop being true. |
| `overlay` | `cray-mpich-abi` \| `host-openmpi` \| `none` | yes |  | Which recipe in libexec/make_apptainer_launcher.sh swaps the host MPI in. A NAME, not a description: the recipe body encodes reasoning -- which of Cray's two library directories carries the MPICH ABI, why OPAL_PREFIX must be pinned -- and reasoning does not belong in a data file. |

###### `site.mpi.<name>.env`

Environment set inside the container for this family. Where a workaround whose reason is specific to this machine goes.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `<name>` | string, integer or boolean |  | key matches `/^[A-Za-z_][A-Za-z0-9_]*$/` | The value, passed through to --env as it is written. |

#### `site.node`

What one compute node has. FALLBACKS, not measurements: the job probes lscpu and topology.json carries its answer. These are what can be known before there is a node to ask, which is the only moment an illegal ranks x threads is still cheap to reject.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `cores` | integer | yes | at least 1 | Physical cores per node. |
| `cores_per_l3` | integer |  | at least 1 | Cores sharing one last-level cache -- a CCD on AMD. Never inferred from a placement report: a run that does not fill a node touches only part of each CCD and would under-count, then declare the result optimal. |
| `cores_per_numa` | integer |  | at least 1 | Cores in one NUMA domain. On a machine with NPS4 this is a quarter of a socket, not the whole one. |
| `smt` | integer | yes | at least 1 | Hardware threads per physical core. |
| `smt_stride` | integer |  | at least 1 | Under a block SMT layout, the sibling of CPU c is c + stride. Equal to cores on a machine that enumerates all first siblings before any second. |
| `sockets` | integer |  | at least 1 | Populated sockets per node. Used to derive cores per socket, which is the granularity a rank should not straddle. |
| `target_arch` | string |  | matches `/^[a-z0-9][a-z0-9._-]*$/` | The instruction set this hardware actually runs, in the spelling report_cpu_features prints. Checked against the app binary once at job start, which turns a SIGILL on every rank three hours into a queue into one line before the first cell. Deliberately a check and not an input: the image was built in CI long before this file was read, so a site can only refuse to measure an image, never change it. |
| `topology` | `probe` \| `declared` |  | default `"probe"` | probe means lscpu on the compute node is the authority and the values above are only a last resort. declared means trust them, which is for a machine where lscpu is not available or not truthful. |

#### `site.scheduler`

Which batch system, and how a job reaches it.

| key | type | required | constraints | meaning |
| --- | --- | --- | --- | --- |
| `kind` | `pbspro` \| `slurm` | yes |  | Selects sites/<site>/job.tmpl's directive dialect. Only pbspro is exercised today; slurm is named so the enum rejects a typo rather than accepting an unimplemented value silently. |
| `queue` | string | yes |  | Default queue. An experiment's defaults.queue overrides it, and so does BENCH_QUEUE in the environment. |
| `submit` | string | yes | matches `/^[a-z][a-z0-9_.-]*$/` | The submission command, qsub or sbatch. |
| `walltime_max` | string |  | matches `/^[0-9]{1,3}:[0-5][0-9]:[0-5][0-9]$/` | The longest walltime this queue accepts. bench/validate refuses a longer request at submit time instead of letting the scheduler reject it after the fact. |
