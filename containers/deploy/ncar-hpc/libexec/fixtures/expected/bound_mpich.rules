# -ppn 8 --cpu-bind core -d 16, OMP_PLACES=cores: the good case.
# Nothing may fire.  A rule that starts firing here is a false positive, which
# is worse than a missed one -- it makes the checker ignorable.
